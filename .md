Add dashboard “time at a glance” quick-view summary.

~Add total duration counter for playlists.~ (This is already implemented. Just needs better accessibility.)

Improve readability with larger text, better contrast, and accessible UI colors.

Standardize inconsistent input/component sizing (e.g., API key field width).

Swap button hierarchy colors: Log Out darker, Delete Account lighter/properly styled.

Make logo and branding consistent.

Normalize playlist badges/tags styling.

Replace “Created At” and “Updated At” with clearer wording.

Make metadata human-readable.

Make log descriptions easier to understand.

# Show recently created playlists first or add sorting.

# Improve playlist selection UX; replace dropdowns for large numbers of playlists/displays with searchable or scalable selectors. Support 100+ playlist scalability use case.

Auto-delete/archive old playlists after a set time.

Add visible timer/counter for currently displayed content on monitors.

## Show how long each content item has been active.

# Allow scheduling immediately (“Now”) instead of requiring future start time.

# Allow scheduling by display groups.

Add display target groups in targeting options.

Add content templates for structured announcements.

### Add Duplicate Content button.

# Allow MP4 looping, especially when only one media item exists.

Add Emergency category in content filters.

Create ready-made emergency broadcast templates.

# Allow selecting emergency type (fire, earthquake, lockdown, etc.).

## Push emergency content to selected displays/groups without manual per-display setup.

Add data import tools (database, JSON, other formats).

Add bulk remove/reset all data option.

Add backup/restore or migration tools.

Add author/user search.

Show specific monitor/device heartbeat identity instead of generic author label.

# Implement server-side caching for performance.
